<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  Export settings for `flutter build ipa`.

	  teamID is read from the APPLE_TEAM_ID secret at build time by the publish
	  workflow, which rewrites this key. It is left as a placeholder here so the
	  file can live in the repository without carrying an account identifier.
	-->
	<key>method</key>
	<string>app-store-connect</string>

	<key>teamID</key>
	<string>$(APPLE_TEAM_ID)</string>

	<!--
	  Manual, and pinned to the profile that was installed.

	  Automatic export asks Xcode to find a profile through a signed-in
	  account. A runner has none, so it fails with "No Accounts" even though
	  the archive itself signed correctly a moment earlier. The workflow
	  substitutes the installed profile's UUID below.
	-->
	<key>signingStyle</key>
	<string>manual</string>

	<key>signingCertificate</key>
	<string>Apple Distribution</string>

	<key>provisioningProfiles</key>
	<dict>
		<key>com.mumbleway.mumbleway</key>
		<string>$(PROVISIONING_PROFILE_UUID)</string>
	</dict>

	<key>uploadSymbols</key>
	<true/>

	<!-- Bitcode has been removed from the toolchain; leaving this true makes
	     newer Xcode versions warn on every export. -->
	<key>uploadBitcode</key>
	<false/>

	<key>destination</key>
	<string>export</string>
</dict>
</plist>
